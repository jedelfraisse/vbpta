@page "/organizations"
@using SiteEngine.Entities
@using WebApp.Services
@inject DashboardService DashboardService

<PageTitle>Organization Types — Central Portal</PageTitle>

<div class="container py-4">
    <div class="text-center mb-5">
        <h1 class="display-6 fw-bold">Organization Types</h1>
        <p class="lead col-lg-8 mx-auto">
            Central Portal supports many kinds of community-driven organizations, each with its
            own structure, leadership, and goals. These are the organization types currently
            supported by the platform, with more to come as the community grows.
        </p>
    </div>

    @if (!_loaded)
    {
        <p class="text-muted text-center">Loading&hellip;</p>
    }
    else if (_organizationTypes.Count == 0)
    {
        <p class="text-muted text-center">Nothing to show yet.</p>
    }
    else
    {
        <div class="row g-4">
            @foreach (var org in _organizationTypes)
            {
                <div class="col-md-6">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body">
                            <h2 class="h4 card-title">
                                @if (!string.IsNullOrWhiteSpace(org.IconClass))
                                {
                                    <i class="@org.IconClass me-2" aria-hidden="true"></i>
                                }
                                @org.Name
                            </h2>
                            <p class="card-text">@org.Description</p>
                        </div>
                    </div>
                </div>
            }
        </div>
    }

    <div class="text-center mt-5">
        <p class="text-muted">Don't see your kind of group here yet? We're still growing.</p>
        <a class="btn btn-primary" href="/unit-sites">Browse the Community Directory</a>
    </div>
</div>

@code {
    private bool _loaded;
    private List<OrganizationType> _organizationTypes = new();

    protected override async Task OnInitializedAsync()
    {
        _organizationTypes = await DashboardService.GetOrganizationTypesAsync();
        _loaded = true;
    }
}
